# ──────────────────────────────────────────────────────────────
# Amigo Voice Assistant — Python Dependencies
# Install: pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────

# ── Speech Recognition (STT) ──────────────────────────────────
SpeechRecognition>=3.10.0      # Voice command microphone transcription
pyaudio>=0.2.14                # Microphone audio capture backend

# ── Neural Text-to-Speech (TTS) ───────────────────────────────
kokoro-onnx>=0.3.1             # Primary neural TTS engine — fast, local, offline
sounddevice>=0.4.6             # Real-time audio playback backend
numpy>=1.24.0                  # Audio array processing for Kokoro ONNX
pyttsx3>=2.90                  # SAPI / Native Windows speech fallback
pywin32>=306                   # win32com SAPI and Windows shell integrations

# ── Local LLM Engine (Qwen 2.5 3B Instruct) ───────────────────
llama-cpp-python>=0.2.80       # Local GGUF quantized model inference (CPU/CUDA)
huggingface-hub>=0.23.0        # Automatic model downloading from HuggingFace

# ── Web Dashboard & REST API ──────────────────────────────────
Flask>=3.0.0                   # Web dashboard, SSE event streaming, REST backend
requests>=2.31.0               # Live weather fetching (wttr.in)

# ── Desktop & OS Automation ───────────────────────────────────
pyautogui>=0.9.54              # Keyboard shortcuts, typing, screen capture
psutil>=5.9.0                  # CPU, RAM, battery telemetry metrics
screen-brightness-control>=0.23.0  # Windows display brightness adjustments
comtypes>=1.4.0                # COM interface for pycaw
pycaw>=20240210                # Windows Core Audio endpoint volume management

# ── Screen Vision & OCR ───────────────────────────────────────
Pillow>=10.0.0                 # Image manipulation & screenshot buffers
winocr>=0.0.15                 # Native Windows 10/11 OCR engine
pytesseract>=0.3.10            # Tesseract OCR fallback

# ── Clipboard & Content Integrations ──────────────────────────
pyperclip>=1.8.2               # Clipboard intelligence
wikipedia>=1.4.0               # Wikipedia encyclopedia search tool
